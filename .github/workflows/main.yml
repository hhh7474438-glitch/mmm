name: Build Hussain Dylib
on: [push, pull_request]

jobs:
  build:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Dependencies
        run: |
          brew install ldid
          # التأكد من أن ldid متاح للنظام
          ln -s /usr/local/bin/ldid /usr/local/bin/sign
          
      - name: Setup Theos
        run: |
          git clone --recursive https://github.com/theos/theos.git ~/theos
          echo "THEOS=~/theos" >> $GITHUB_ENV
          echo "$HOME/theos/bin" >> $GITHUB_PATH

      - name: Install SDK
        run: |
          mkdir -p ~/theos/sdks
          curl -LO https://github.com/theos/sdks/archive/master.zip
          unzip -q master.zip
          mv sdks-master/*.sdk ~/theos/sdks/
          rm -rf master.zip sdks-master

      - name: Build Tweak
        run: |
          export THEOS=~/theos
          # تخطي التوقيع داخل Theos لأننا سنقوم به يدوياً لاحقاً أو عبر Sideloadly
          make FINALPACKAGE=1 CODESIGN_IPA=0 
        env:
          THEOS: ~/theos

      - name: Prepare Artifacts
        run: |
          mkdir -p output
          # البحث عن ملف dylib الناتج ونقله
          find .theos/obj/ -name "*.dylib" -exec cp {} output/ \;

      - name: Upload Dylib
        uses: actions/upload-artifact@v4
        with:
          name: Hussain-Edition-Dylib
          path: output/*.dylib
